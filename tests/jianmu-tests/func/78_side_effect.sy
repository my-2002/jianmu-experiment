
int a = -1, b = 1;

int inc_a()
{
    int b = a;
    b = b + 1;
    a = b;
    return a;
}

int main()
{
    int k = 5;
    while (k >= 0) {
        if (inc_a() && inc_a() && inc_a()) {
            putint(a); putch(32); putint(b); putch(10);  //32是空格，10是换行
            putint(111);
        }
        if (inc_a() < 14 || inc_a() && inc_a() - inc_a() + 1) {
            putint(a); putch(10);
            b = b * 2;
            putint(222);
        } else {
            inc_a();
            putint(333);
        }
        k = k - 1;
        putch(10);
        putch(97);
        putint(a);
        putch(10);
    }
    putint(a); putch(32); putint(b); putch(10);
    return a;
}
